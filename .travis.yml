sudo: required

language: node_js

node_js:
  - 7

services:
  - docker

before_install:
  - npm -v
  - node -v
  - docker version
  - docker-compose version
  - npm cache clean

install:
  - npm install

test:
  - docker-compose ps
  - npm test
